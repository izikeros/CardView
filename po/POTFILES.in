# List of source files containing translatable strings
# This file is used by xgettext to extract strings for translation

# Core plugin files
src/card_view.py
src/card_view_citation.py
src/card_view_event.py
src/card_view_family.py
src/card_view_media.py
src/card_view_note.py
src/card_view_person.py

# Common modules
src/view/common/common_strings.py
src/view/common/hybrid_localization.py

# Card components (will be expanded in later stages)
# src/view/cards/*.py

# Services (will be expanded in later stages)
# src/view/services/*.py

# Forms and dialogs (will be expanded in later stages)
# src/view/dialogs/*.py
# src/view/forms/*.py

# Configuration (will be expanded in later stages)
# src/config*.py
# src/view/config/*.py

# Actions and menus (will be expanded in later stages)
# src/view/actions/*.py
# src/view/menus/*.py

# Utilities and helpers (will be expanded in later stages)
# src/view/utilities/*.py
# src/view/helpers/*.py
